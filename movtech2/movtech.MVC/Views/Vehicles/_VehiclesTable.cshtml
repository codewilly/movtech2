@*@model IEnumerable<movtech.Domain.Entities.Vehicle>

<style>
    .action-link {
        margin: 0 4px;
        color: #577399 !important;
    }
</style>


<table class="table table-striped table-sm text-center">

    <thead>
        <tr>
            <th>Placa</th>
            <th>Marca</th>
            <th>Modelo</th>
            <th>Motorista</th>
            <th>Ação</th>
        </tr>
    </thead>

    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>@item.LicensePlate</td>
                <td>@item.Brand</td>
                <td>@item.Model</td>
                <td>-</td>
                <td><a href="#" data-toggle="tooltip" data-placement="bottom" title="Detalhes" class="action-link"><span class="fas fa-search"></span></a> <a href="#" class="action-link"><span class="fas fa-pencil-alt"></span></a></td>
            </tr>
        }

    </tbody>

</table>

<script>
    $(document).ready(function () {
        $('[data-toggle="tooltip"]').tooltip();
    });
</script>*@